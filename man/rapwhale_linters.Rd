% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rapwhale_linters.R
\name{rapwhale_linters}
\alias{rapwhale_linters}
\title{Standardlinterar}
\usage{
rapwhale_linters(...)
}
\arguments{
\item{...}{Linterar som skal leggjast til eller erstatta standardlinterane,
til dømes \code{lintr::line_length_linter(100)},
eller \code{namn_linter = NULL} for å slå av ein linter.
Sjå \code{\link[lintr:modify_defaults]{lintr::modify_defaults()}}.}
}
\value{
Ei namngjeven liste med linterar,
til bruk i \code{.lintr}-filer eller i \code{\link[lintr:lint_package]{lintr::lint_package()}}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Gjev linterane som svarar til kodestilguiden vår,
slik at alle pakkane på Rapporteket kan bruka same reglar.
}
\details{
Funksjonen er meint for bruk i \code{.lintr}-filer:

\if{html}{\out{<div class="sourceCode">}}\preformatted{linters: rapwhale::rapwhale_linters()
}\if{html}{\out{</div>}}

Utgangspunktet er alle linterane i lintr-pakken
(\code{\link[lintr:all_linters]{lintr::all_linters()}}),
med nokre slegne av og nokre tilpassa kodestilguiden vår,
i tillegg til \code{\link[=fixme_stil_linter]{fixme_stil_linter()}}.

Treng ein pakke unntak frå standarden,
kan ein leggja dei til som argument,
slik at det går tydeleg fram kva som er annleis:

\if{html}{\out{<div class="sourceCode">}}\preformatted{linters: rapwhale::rapwhale_linters(
    line_length_linter(100),
    undesirable_function_linter = NULL
  )
}\if{html}{\out{</div>}}
}
\examples{
# Standardlinterane
names(rapwhale_linters())

# Med kortare linjer og utan linteren for uønskte funksjonar
lintr::lint(
  text = "x = lapply(1:3, sqrt)",
  linters = rapwhale_linters(
    lintr::line_length_linter(80),
    undesirable_function_linter = NULL
  )
)
}
